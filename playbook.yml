---
- name: configure web
  hosts: web
  remote_user: rusik
  become: yes
  vars_files:
    - vars/default.yml
  roles:
    - web

- name: configure db
  hosts: db
  remote_user: rusik
  become: yes
  vars_files:
    - vars/default.yml
  roles:
    - db